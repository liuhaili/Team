using UnityEngine;
using System.Collections;
using UnityEngine.EventSystems;
using UnityEngine.UI;
using Lemon.Team.Entity;
using System.Collections.Generic;

public class NewBehaviourScript : MonoBehaviour
{
    //public NewListViewControl ListView;
    void Start()
    {
        //List<object> plist = new List<object>();
        //for (int i = 0; i < 1000; i++)
        //{
        //    plist.Add(new EUser() { ID = i, Name = "name" + i, Phone = "phone" + i });
        //}

        //ListView.BindData("PeopleItem", plist, (g, t) =>
        //{
        //    EUser user = t as EUser;
        //    g.name = "PeopleItem_" + user.ID;
        //    g.transform.Find("Name").GetComponent<Text>().text = user.Name;
        //    g.transform.Find("Phone").GetComponent<Text>().text = user.Phone;
        //});
    }

    void Update()
    {

    }
}
